# 公共资源目录

用于存放仓库根目录下可被多个模块复用的资源，不绑定某一个端或某一个学科。

详细规范见：

- `docs/plans/公共资源目录规范.md`

当前约定：

- `brand/`：品牌与视觉识别资源，例如 logo、配色、图标基准稿
- `illustrations/`：跨模块可复用插画、装饰图形
- `subjects/`：按学科沉淀的公共资源底稿

使用建议：

- 这里保存源文件与主稿
- 各业务模块按需要引用或导出，不要在多个模块里各自维护一份母版
- 后续若新增一级分类，需要同步更新本目录说明和规范文档
